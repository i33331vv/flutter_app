name: Build Android App Bundle (AAB)

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0' # تم تثبيت إصدار مستقر يتوافق تماماً مع إعدادات مشروعك
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build App Bundle (AAB)
        run: flutter build appbundle --release
